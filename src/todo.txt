- review and correct comments in code then unit (white box) tests and integration (black box) tests
- test multiple sites on single host
- avoid redownloading terraform provider plugins for already existing plugins
- How to handle ssl renewals? Possibly rerunning 'page up' should renew ssl?
- How to handle cname record updates? By default root and www subdomain should reach the main website.
- add additional tests
- cli assumes static assets to be uploaded contains index.html. Validate or change.
- Add user output for exactly what resources were being/going to be deployed to my host and how much its going to cost me. If possible I'd like the ability to revert/destroy the deployment.
- run gitlab ci test suite for each supported os/architecture